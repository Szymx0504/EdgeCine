[pytest]
pythonpath = .
testpaths = tests
# Suppress common third-party warnings to keep output clean for recruiters
filterwarnings =
    ignore::DeprecationWarning:pydantic.*:
    ignore::DeprecationWarning:fastapi.*:
    ignore::UserWarning:torch.*:
    ignore::RuntimeWarning:httpx.*:
